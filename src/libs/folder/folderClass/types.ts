export interface FolderEntry {
  indexHTML: boolean;
  gameRmmzProject: boolean;
}
export interface DataFolder {
  actors: string;
  enemies: string;
  items: string;
  skills: string;
  weapons: string;
  armors: string;
  classes: string;
  states: string;
  system: string;
  commonEvents: string;
}
